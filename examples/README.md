# Examples

This directory contains optional materials that are not part of the reusable pipeline surface.

- `svhn_local_analysis/`: local post-processing helper for one SVHN-specific analysis workflow

The core package and the canonical report-flow example do not depend on these files.
